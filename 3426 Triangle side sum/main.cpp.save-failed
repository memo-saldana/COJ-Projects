#include <stdio.h>
#include <cmath>
#include <algorithm>
using namespace std;

int main()
{
    int x, y,ans;

    scanf("%d %d",&x,&y);
    int imin=abs(x-y);

    ans=((x+y-1)*(1+x+y-1)/2)-((imin*(imin+1))/2);

    printf("%d", ans);

    return 0;
}
