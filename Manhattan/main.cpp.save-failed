#include <stdio.h>
using namespace std;

int main()
{
  int T, X1, Y1, X2, Y2, Res;
  scanf("%i",&T);

  for(int i=0; i<T; i++)
  {
    scanf("%i %i %i %i", &X1, &Y1, &X2, &Y2);
    Res=(X2-X1)+(Y2-Y1);
    printf("%i",Res);
  }
  return 0;
}
